# @git-to-uml/parser-java (Phase 3)

Not implemented yet. Will implement `LanguageParser` from `@git-to-uml/ir`
using `web-tree-sitter` + a Java `.scm` query file — explicit visibility
modifiers and extends/implements map directly. See the design plan.
