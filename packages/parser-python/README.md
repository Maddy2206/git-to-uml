# @git-to-uml/parser-python (Phase 3)

Not implemented yet. Will implement `LanguageParser` from `@git-to-uml/ir`
using `web-tree-sitter` + a Python `.scm` query file, with field inference
from `self.x = ...` assignments and visibility inferred from `_`/`__` naming
convention (Python has no visibility keywords). See the design plan.
