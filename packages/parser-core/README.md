# @git-to-uml/parser-core (Phase 3)

Will hold the shared `web-tree-sitter` WASM loader and `.scm` query-running
utilities used by `parser-python` and `parser-java`. Not implemented yet —
Phase 1 only needs `parser-ts` (ts-morph handles TS/JS directly, without
tree-sitter). See the design plan for details.
